<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.ansqun.advancedlight"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:resizeableActivity="false"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ch1.recyclerview.RecyclerViewActivity">
        </activity>
        <activity android:name=".ch1.cardview.CardViewActivity">
        </activity>
        <activity android:name=".ch1.notificatioin.NotificationActivity">
        </activity>
        <activity android:name=".ch1.toolbar.ToolbarActivity">
        </activity>
        <activity android:name=".ch1.permission.PermissionActivity">
        </activity>
        <activity android:name=".ch1.permission.ThirdPartyActivity">
        </activity>
        <activity android:name=".ch1.multiwindow.MultiWindowActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>